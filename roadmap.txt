1. Checker les erreurs, gerer les leaks

- nombres d'arguments
- malloc structure game
- extension de fichier
- mlx_init()
- ouverture de fichier, lecture de map, get_next_line(), open(), close()
- si la map est rectangulaire
- si les caracteres sont correctes
- que des murs autour de la map
- que tout les xpm soit ok mlx_new_window(), mlx_xpm_file_to_image()

en fonction du besoin exit() =>
- windows : mlx_destroy_window()
- mlx : mlx_destroy_display() + free()
- sprites : mlx_destroy_images

free();

2. initialisation structure
- a NULL ou la mlx mlx_init

3. render
- analyser la map et afficher en fonction de l'element mlx_put_image_o_window()

4.interactions avec le clavier
- mlx_key_hook(), choisir une fonction a implementer ex : ft_event_key()

Inserer le code pour :
- autorisation avancer personnage ou pas, et action si ok

bonus : mlx_string_put() : afficher sur l'ecran le nb de mouvement

5. apparaitre les images en boucle
- mlx_loop()
- mlx_hook(), permet d'inserer une fonction pour fermer la fenetre avec la croix
